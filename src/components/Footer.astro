---
import { navLinks, snsLinks } from "@/data/site"
import { Image } from "astro:assets"

// SNSロゴのインポート
import instagramLogo from "../assets/logo/brandlogo-instagram.png"
import lineLogo from "../assets/logo/brandlogo-line.png"
import xLogo from "../assets/logo/brandlogo-x.png"

// SNSロゴのマッピング
const logoMap = {
  Instagram: instagramLogo,
  X: xLogo,
  LINE: lineLogo,
}
---

<footer class="bg-foreground text-background pt-14 pb-7">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-10">
      <!-- 左カラム: 店舗情報 -->
      <div class="space-y-4">
        <h3
          class="text-xl font-webfont mb-5 relative pb-3 after:content-[''] after:absolute after:bottom-0 after:left-0 after:w-16 after:h-0.5 after:bg-primary"
        >
          やきとん酒場
        </h3>
        <p class="max-w-xs text-background/80">
          串とんと日本酒の店「三代目ひょうたん」は、昭和の雰囲気が味わえる、珍しい豚の部位を使用した焼きとんと、入手困難な日本酒を楽しめるお店です。
        </p>

        <!-- SNSリンク -->
        <div class="flex space-x-4 mt-6">
          {
            snsLinks.map((link) => {
              const logo = logoMap[link.name]
              return (
                <a
                  href={link.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  aria-label={link.ariaLabel}
                  class="bg-background rounded-full p-2 hover:bg-primary/20 transition-colors duration-300"
                >
                  {logo && <Image src={logo} alt={link.ariaLabel} width={24} height={24} />}
                </a>
              )
            })
          }
        </div>
      </div>

      <!-- 中央カラム: ナビゲーションリンク -->
      <div class="space-y-4">
        <h3
          class="text-xl font-webfont mb-5 relative pb-3 after:content-[''] after:absolute after:bottom-0 after:left-0 after:w-16 after:h-0.5 after:bg-primary"
        >
          リンク
        </h3>
        <ul class="space-y-2">
          {
            navLinks.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="hover:text-primary transition-colors duration-300 flex items-center"
                >
                  <span class="transform inline-block transition-transform duration-300 mr-2">
                    ›
                  </span>
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <!-- 右カラム: 営業時間・連絡先 -->
      <div class="space-y-4">
        <h3
          class="text-xl font-webfont mb-5 relative pb-3 after:content-[''] after:absolute after:bottom-0 after:left-0 after:w-16 after:h-0.5 after:bg-primary"
        >
          営業時間
        </h3>
        <p class="text-background/80">平日: 17:00 - 23:30（L.O. 23:00）</p>
        <p class="text-background/80">定休日: 毎週日曜日,祝日</p>

        <div class="mt-6">
          <p class="text-background/80">〒683-0055</p>
          <p class="text-background/80">鳥取県米子市富士見町２丁目１５０</p>
          <p class="text-background/80 mt-2">ご予約・お問い合わせ</p>
          <p class="text-xl font-bold text-primary">000-0000-0000</p>
        </div>
      </div>
    </div>

    <!-- コピーライト -->
    <div class="border-t border-background/20 pt-6 text-center text-background/60">
      &copy; {new Date().getFullYear()} 串とん亭日本酒酒場 三代目ひょうたん All Rights Reserved.
    </div>
  </div>
</footer>
